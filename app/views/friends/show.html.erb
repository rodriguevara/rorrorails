<div class="jumbotron">
  <%= render @friend %>

  <%= link_to "Edit this friend", edit_friend_path(@friend), class:"btn btn-secondary" %> |
  <td>
  <%= link_to "Back to friends", friends_path, class:"btn btn-secondary" %>
  <td>
  <%= button_to "Destroy this friend", @friend, form: { data: { turbo_confirm: "Are you sure?" } }, method: :delete,  class:"btn btn-danger" %>
 
</div>


